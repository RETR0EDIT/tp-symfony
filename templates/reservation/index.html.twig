{# filepath: /c:/Users/admin/Desktop/tp symfony/tpsymfo/templates/reservation/index.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Mes Réservations</h1>

    <div>
        <h2>Informations Utilisateur</h2>
        <p><strong>Nom :</strong> {{ user.name }}</p>
        <p><strong>Email :</strong> {{ user.email }}</p>
        <p><strong>Numéro de téléphone :</strong> {{ user.phoneNumber }}</p>
    </div>

    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Heure</th>
                <th>Événement</th>
            </tr>
        </thead>
        <tbody>
            {% for reservation in reservations %}
                <tr>
                    <td>{{ reservation.date|date('Y-m-d H:i') }}</td>
                    <td>{{ reservation.timeSlot }}</td>
                    <td>{{ reservation.eventName }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reservation_new') }}">Nouvelle Réservation</a>
{% endblock %}