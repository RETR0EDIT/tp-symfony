{# filepath: /c:/Users/admin/Desktop/tp symfony/tpsymfo/templates/security/login.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Connexion</h1>

    <form method="post" action="{{ path('app_login') }}">
        {% if error %}
            <div>{{ error }}</div>
        {% endif %}

        <div>
            <label for="email">Email:</label>
            <input type="email" id="email" name="_email" value="{{ last_username }}" required autofocus>
        </div>

        <div>
            <label for="password">Password:</label>
            <input type="password" id="password" name="_password" required>
        </div>

        <button type="submit">Se connecter</button>
    </form>
{% endblock %}